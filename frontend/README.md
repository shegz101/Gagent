# AI Workspace Frontend

This directory will contain the Next.js frontend for the AI Workspace application.

## To be implemented:

- Next.js 14 application
- Dashboard with calendar, email, and task views
- Real-time updates via WebSocket/SSE
- AI agent chat interface
- Tailwind CSS + ShadCN UI components

## Setup (when ready):

```bash
cd frontend
npx create-next-app@latest . --typescript --tailwind --app
npm install
npm run dev
```

For now, focus on backend development and testing.
